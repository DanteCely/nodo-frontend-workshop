function cuadratica(a, b, esSuma) {
  const dentroRaiz = Math.pow(b) - 4 * a * c;
  const denominador = 2 * a;
  let resultado;

  if (esSuma === true) {
    resultado = numerador + Math.SQRT2(dentroRaiz);
  } else {
    resultado = numerador - Math.SQRT2(dentroRaiz);
  }

  return resultado;
}
